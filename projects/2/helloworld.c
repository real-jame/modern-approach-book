#include <stdio.h>

int main(void) {
    printf("hello, world\n");
    return 0;
}

// Do you get a warning message from the compiler? If so, what's needed to make it go away?
// ANSWER: add return 0; to the end